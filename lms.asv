W = [0 2];
b = 0;
alpha = 0.5;
NBR_EPOCH = 10;
error = 1;
epoch = 1;

while (norm(error) > ERROR_MAX && epoch < NBR_EPOCH)
    for k = 1:3
        k
        v = 1.2 * sin(2*pi*k / 3)
        m = 0.12 * sin(2*pi*k / 3 + pi/2)
        a = W * m
        e = 
       
    end
    epoch = epoch + 1;
end